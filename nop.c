#include "monty.h"
/**
 * nop_opcode - divide the first two element of the stack
 * @stack_head: stack head top
 * @check: numbers of line occupied
 * Return: no return
 */
void nop_opcode(stact_t **stack_head, unsigned int check)
{
	(void) check;
	(void) stack_head;
}
