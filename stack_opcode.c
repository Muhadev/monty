#include "monty.h"
/**
 * stack_opcode - print the top of the stack
 * @stack_head: stack head top
 * @check: numbers of line occupied
 * Return: no return
 */
void stack_opcode(stack_t **stack_head, unsigned int check)
{
	(void) check;
	(void) stack_head;
	pack.value = 0;
}
